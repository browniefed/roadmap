var LayoutManager = require('./LayoutManager'),
    moment = require('moment');


var DateInterace = {

    dateToLayoutFields: function(_scale, start, end) {
        var mStart = moment(start),
            mEnd = moment(end),
            scale = _scale || {};

        var startDiff = (moment(scale.start).diff(mStart, 'days') * -1),
            endDiff = (momemt(scale.end).diff(mEnd, 'days') * -1);

        return {
            x: startDiff,
            w: endDiff - startDiff
        }
    }

}

return DateInterace;